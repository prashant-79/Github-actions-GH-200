name: run container action
description: A simple container action that greets the user

on:
  push:
    branches:
      - main
  workflow_dispatch:    

jobs:
  run-container-action:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3

      - name: run my container action
        uses: prashant-79/Github-actions-GH-200@main
        with:
          name: "Prashant"
          
